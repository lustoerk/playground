tmp

# Ansible
ansible/hosts.ini
ansible/sa_key.json
ansible/ansible-controller-sa-key.json

# Terraform
terraform/.terraform/

## AWS
# Local .terraform directories
terraform/aws/.terraform/
# .tfstate files
terraform/aws/*.tfstate
terraform/aws/*.tfstate.*
# Crash log files
terraform/aws/crash.log
terraform/aws/crash.*.log
# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version 
# control as they are data points which are potentially sensitive and subject 
# to change depending on the environment.
terraform/aws/*.tfvars
terraform/aws/*.tfvars.json
# Ignore override files as they are usually used to override resources locally and so
# are not checked in
terraform/aws/override.tf
terraform/aws/override.tf.json
terraform/aws/*_override.tf
terraform/aws/*_override.tf.json
# Ignore transient lock info files created by terraform apply
terraform/aws/.terraform.tfstate.lock.info

# Include override files you do wish to add to version control using negated pattern
# !example_override.tf

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
# example: *tfplan*

# Ignore CLI configuration files
terraform/aws/.terraformrc
terraform/aws/terraform.rc

# SSH Key
terraform/aws/aws-key-pair.pem


## GCO
# Local .terraform directories
terraform/gcp/.terraform/
# .tfstate files
terraform/gcp/*.tfstate
terraform/gcp/*.tfstate.*
# Crash log files
terraform/gcp/crash.log
terraform/gcp/crash.*.log
# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version 
# control as they are data points which are potentially sensitive and subject 
# to change depending on the environment.
terraform/gcp/*.tfvars
terraform/gcp/*.tfvars.json
# Ignore override files as they are usually used to override resources locally and so
# are not checked in
terraform/gcp/override.tf
terraform/gcp/override.tf.json
terraform/gcp/*_override.tf
terraform/gcp/*_override.tf.json
# Ignore transient lock info files created by terraform apply
terraform/gcp/.terraform.tfstate.lock.info
# Include override files you do wish to add to version control using negated pattern
# !example_override.tf
# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
# example: *tfplan*
# Ignore CLI configuration files
terraform/gcp/.terraformrc
terraform/gcp/terraform.rc
